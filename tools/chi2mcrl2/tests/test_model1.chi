model blaat()=
|[ blaak() || test(false, true)
]|

proc blaak()=
|[ var a: bool
:: skip ;*( a := false )
]|

proc test(var b,a:bool)=
|[
a *> ( a -> skip ; delta | skip ; skip ; a := false ) ; skip
]|

