model tst()=
|[
dummy()
]|

proc dummy()=
|[  var a: (nat, bool)
 ,      b: nat
 ,      c: bool
::  a := (1, true);
    b := a.0;
    c := a.1;
    a = (3,false) -> skip;
    (1,2) = (2,3) -> skip
]|

