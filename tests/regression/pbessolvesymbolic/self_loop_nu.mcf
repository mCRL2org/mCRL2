% There is an infinite path. On self_loop.mcrl2 this holds, and the self-dependency is by itself a
% winning move for the disjunctive player, so the witness is a single state with a self-loop.
nu X. <true>X
