project aterm
       : source-location ../source
       : build-dir $(BUILD_TOP)
       ;

alias atermpp
       :
       : <use>/boost//headers
       :
       : <use>/boost//headers
         <dependency>/libraries//aterm
       ;
