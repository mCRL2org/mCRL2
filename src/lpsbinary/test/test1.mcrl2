% lpsbinary Test Case 1 -- variable of sort can be coded into 1 variable 
% of type bool
%
% Process parameter e (of sort D) can be encoded into one variable of type bool
% and should be replaced in nextstate.
% The initial state should be altered accordingly.

sort
  D = struct d1 | d2;

act a,b;

proc X(e:D) = sum d:D . a . b . X(d);
init X(d1);
