% lpssumelm Test Case 1 -- A simple constructor sort (1 level) only occurs in
% a sum
%
% sort D only occurs in a sum, the sum can be unfolded.
sort D = struct d1|d2;

act a,b;

proc X = sum d:D . a . b . X;

init X;
