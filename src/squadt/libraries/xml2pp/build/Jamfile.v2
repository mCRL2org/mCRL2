project xml_text_reader
  : source-location ../source
  : build-dir $(BUILD_TOP)
  : requirements
    <include>../../
  : usage-requirements
    <include>../../
    <cxxflags>"`xml2-config --cflags`"
    <linkflags>"`xml2-config --libs`" ;

obj xml_text_reader : xml_text_reader.cpp
                    : <cxxflags>"`xml2-config --cflags`" ;
