project text_reader
    : source-location ../source
    : build-dir $(BUILD_TOP)
    : requirements
       <include>$(BOOST)
       <include>../..
    : usage-requirements
       <include>../..
    ;

include $(TOP)/config.jam ;

obj text_reader
    : exception.cpp
    : # no includes
    :
    : <cxxflags>"$(XML2_CPPFLAGS)"
      <linkflags>"$(XML2_LDFLAGS)"
    ;
