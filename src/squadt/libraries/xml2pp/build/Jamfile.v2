project text_reader
    : source-location
       ../source
    : build-dir
       .
    : requirements
       <include>$(BOOST)
       <include>../../
    : usage-requirements
       <include>../../
    ;

include $(TOP)/config.jam ;

obj text_reader
    : exception.cpp
    : # no includes
    :
    : <cxxflags>"$(IXML2)"
      <linkflags>"$(LXML2)"
    ;
