project text_reader
    : source-location
       ../source
    : build-dir
       $(BUILD_TOP)
    : requirements
       <include>../../
    : usage-requirements
       <include>../../
    ;

include $(TOP)/config.jam ;

alias text_reader
    : # no sources
    : # no includes
    :
    : <cxxflags>"$(IXML2)"
      <linkflags>"$(LXML2)"
    ;
