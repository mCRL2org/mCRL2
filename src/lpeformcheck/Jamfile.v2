project lpeformcheck
  : source-location .
  : build-dir $(BUILD_TOP)
  : default-build debug
  ;

exe lpeformcheck
       : lpeformcheck.cpp
         /libraries/prover//prover
       : <source>/root//mcrl2_revision
         <include>.
       ;

include $(TOP)/config.jam ;

explicit install install-libraries install-binaries ;

alias install
       : install-libraries
         install-binaries
       ;

install install-binaries
       : lpeformcheck
       : <dll-path>$(LIB_DIR)
         <location>$(BIN_DIR)
       ;

install install-libraries
      : lpeformcheck
      : <install-dependencies>on <install-type>LIB
      : <location>$(LIB_DIR)
      ;

