% Model(s): {abp.mcrl2, abp_bw.mcrl2}
%
% Description: All messages dd are (once they have been read through action r1)
%              inevitably sent through action s4, if not permanently lost (via
%              internal action i).
%


nu X.
(
[true]X

&&

forall dd:D.
             ([r1(dd)]( nu Y. mu Z. ([(!s4(dd)) && (!i)]Z && [i]Y) ) )
)
